import { randomPlayer } from "./randomPlayer";
import { randomWristband } from "./randomWristband";

/**
 * generateWristbandPairedPlayers
 *
 * @param {number || Array<{...player, wristbandNumber}>} players
 */
function generateWristbandPairedPlayers(count) {
  return new Promise((resolve, reject) => {});
  Promise.all();
}

export { generateWristbandPairedPlayers };
